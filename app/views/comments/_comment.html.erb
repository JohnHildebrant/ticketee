<% coder = HTMLEntities.new %>

<%= div_for(comment) do %>
  <h4><%= comment.user %></h4>
  <%= state_for(comment) %>
  <%= simple_format(coder.encode(comment.text.gsub("\r\n", "\n"), :named)) %>
<% end %>